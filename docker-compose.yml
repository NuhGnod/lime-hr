version: '3.2'
services:
  web:
    container_name: hrm_web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    stdin_open: true
    tty: true
    restart: always
networks:
  defalut:
    external:
      name: hrm_network
